import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'us.tke.ninety_percent',
  appName: '90%',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
